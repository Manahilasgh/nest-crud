import { Module } from '@nestjs/common';

/**
 * CommonModule contains shared decorators, guards, filters, interceptors, and middleware.
 * This demonstrates modular organization in NestJS - grouping cross-cutting concerns.
 */
@Module({})
export class CommonModule {}
